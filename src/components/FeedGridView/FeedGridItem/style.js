import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  mainGridView: {flex: 1, maxWidth: `${100 / 3}%`, aspectRatio: 1, padding: 1},
  imageStyle: {flex: 1},
  iconStyle: {
    position: 'absolute',
    top: 5,
    right: 5,
  },
});
